import*as rls from "readline-sync";
let numeromaximo:number=rls.questionInt("ingrese numero:");
let i:number=0;
while (numeromaximo !== 0); {
    if(i <= numeromaximo) {
    i=numeromaximo;}
    i=numeromaximo;}
    i=numeromaximo;{
   
    numeromaximo=rls.questionInt("ingrese numero:");
    numeromaximo=rls.questionInt("el numero maximo es:");
}
console.log("el numero maximo es:"+i);
